import tagSplitter from '../../../utils/tag-splitter';
import { module, test } from 'qunit';

module('Unit | Utility | tag splitter');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = tagSplitter();
  assert.ok(result);
});
